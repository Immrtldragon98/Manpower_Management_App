import { cookieValue,sign,verifyWorkerToken } from "@/lib/security";import { requireAdmin } from "@/lib/admin";import { query } from "@/db";
const today=()=>new Date().toISOString().slice(0,10);async function dailyCode(secret:string){return (await sign(`plant-operations:${today()}`,secret)).slice(0,6).toUpperCase()}
export async function GET(){const admin=await requireAdmin();if(!admin)return Response.json({error:"Admin sign-in required."},{status:401});const secret=process.env.SESSION_SECRET||"";const data=await query('SELECT a.id,a.attendance_date AS "attendanceDate",a.requested_at AS "requestedAt",a.status,m.name,m.employee_id AS "employeeId",m.contractor,m.shift FROM attendance a JOIN manpower m ON m.id=a.manpower_id ORDER BY a.requested_at DESC');return Response.json({records:data.rows,workplaceCode:await dailyCode(secret)})}
export async function POST(request:Request){const secret=process.env.SESSION_SECRET||"";const id=await verifyWorkerToken(cookieValue(request,"mp_session"),secret);if(!id)return Response.json({error:"Sign in again."},{status:401});const body=await request.json() as {code?:string};if((body.code||"").toUpperCase()!==await dailyCode(secret))return Response.json({error:"Workplace QR is invalid or expired."},{status:400});try{await query("INSERT INTO attendance (manpower_id,attendance_date,requested_at,status) VALUES ($1,$2,$3,$4)",[id,today(),new Date().toISOString(),"Pending"]);return Response.json({ok:true},{status:201})}catch{return Response.json({error:"Attendance was already requested today."},{status:409})}}
export async function PATCH(request:Request){const admin=await requireAdmin();if(!admin)return Response.json({error:"Admin sign-in required."},{status:401});const body=await request.json() as {id?:number;status?:string};if(!body.id||!["Approved","Rejected"].includes(body.status||""))return Response.json({error:"Invalid request."},{status:400});await query("UPDATE attendance SET status=$1,reviewed_at=$2,reviewed_by=$3 WHERE id=$4",[body.status,new Date().toISOString(),admin.email,body.id]);return Response.json({ok:true})}
