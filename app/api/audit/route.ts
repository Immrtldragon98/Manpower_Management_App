import { requireAdmin } from "@/lib/admin";import { query } from "@/db";
export async function GET(request:Request){const admin=await requireAdmin();if(!admin||admin.role!=="Company Admin")return Response.json({error:"Company admin access required."},{status:403});const url=new URL(request.url),limit=Math.min(Number(url.searchParams.get("limit"))||100,500);const r=admin.companyId?await query('SELECT id,actor_type AS "actorType",actor_id AS "actorId",action,entity_type AS "entityType",entity_id AS "entityId",summary,created_at AS "createdAt" FROM audit_logs WHERE company_id=$1 ORDER BY id DESC LIMIT $2',[admin.companyId,limit]):await query('SELECT id,actor_type AS "actorType",actor_id AS "actorId",action,entity_type AS "entityType",entity_id AS "entityId",summary,created_at AS "createdAt" FROM audit_logs ORDER BY id DESC LIMIT $1',[limit]);return Response.json(r.rows)}
