"use client";import { FormEvent,useState } from "react";import { HardHat,LogIn } from "lucide-react";
export default function AdminLogin(){const [error,setError]=useState(""),[busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const r=await fetch("/api/admin/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(Object.fromEntries(new FormData(e.currentTarget)))});const out=await r.json();setBusy(false);if(!r.ok)return setError(out.error);location.reload()}return <div className="loginpage"><section className="loginbox"><div className="brand dark"><span><HardHat/></span><div><b>Workforce Hub</b><small>ADMIN</small></div></div><h1>Admin sign in</h1><p>Use your authorized email and admin password.</p>{error&&<div className="formerror">{error}</div>}<form onSubmit={submit}><label className="field"><span>Email</span><input name="email" type="email" required/></label><label className="field"><span>Password</span><input name="password" type="password" required/></label><button className="primary" disabled={busy}><LogIn/>{busy?"Signing in…":"Sign in"}</button></form><a href="/">Worker sign in</a></section></div>}
