import { cookies } from "next/headers";
import { verifyAdminToken,verifyStaffToken } from "@/lib/security";
import { query } from "@/db";
export type StaffSession={id:number|null;email:string;name:string;role:"Company Admin"|"Manager"|"Supervisor"|"HR"|"Safety Officer";companyId:number|null;scopeType:string;scopeId:number|null;manpowerId:number|null;[key:string]:unknown};
export async function requireAdmin():Promise<StaffSession|null>{const secret=process.env.SESSION_SECRET||"";const token=(await cookies()).get("admin_session")?.value;if(await verifyAdminToken(token,secret))return {id:null,email:process.env.ADMIN_EMAIL||"",name:"Company Admin",role:"Company Admin",companyId:null,scopeType:"Company",scopeId:null,manpowerId:null};const verified=await verifyStaffToken(token,secret);if(!verified)return null;const r=await query<StaffSession>('SELECT s.id,s.email,s.name,s.role,s.company_id AS "companyId",s.scope_type AS "scopeType",s.scope_id AS "scopeId",s.manpower_id AS "manpowerId" FROM staff_accounts s WHERE s.id=$1 AND s.active=TRUE',[verified.id]);return r.rows[0]||null}
export function canManage(session:StaffSession,area:"people"|"schedule"|"work"|"safety"){if(session.role==="Company Admin"||session.role==="Manager")return true;if(session.role==="HR")return area==="people"||area==="schedule";if(session.role==="Supervisor")return area!=="safety";return area==="safety"}
export function scopeFilter(session:StaffSession,alias="m"){if(session.role==="Manager"&&session.id)return {sql:`${alias}.manager_id=$1`,values:[session.id] as unknown[]};if(session.companyId&&(session.role==="Company Admin"||session.scopeType==="Company"||!session.scopeId))return {sql:`${alias}.company_id=$1`,values:[session.companyId] as unknown[]};if(!session.companyId)return {sql:"TRUE",values:[] as unknown[]};const columns:{[key:string]:string}={Plant:"plant_id",Department:"department_id","Sub-department":"subdepartment_id",Discipline:"discipline_id"};return {sql:`${alias}.company_id=$1 AND ${alias}.${columns[session.scopeType]}=$2`,values:[session.companyId,session.scopeId] as unknown[]}}
export function selectedScopeAllowed(session:StaffSession,ids:{plantId:number;departmentId:number;subdepartmentId:number;disciplineId:number}){if(session.role==="Company Admin"||session.scopeType==="Company")return true;const key:{[key:string]:keyof typeof ids}={Plant:"plantId",Department:"departmentId","Sub-department":"subdepartmentId",Discipline:"disciplineId"};return ids[key[session.scopeType]]===session.scopeId}
